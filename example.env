# PostgreSQL (local) - Update with your credentials
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/catalogdb

# SQLite (fallback) - Uncomment to use SQLite instead
# DATABASE_URL=sqlite+aiosqlite:///./dev.db

REDIS_URL=                                      # optional; if empty, fallback used
STORAGE_PATH=./storage
SECRET_KEY=change_me
PORT=10000
PURGE_DAYS=30

